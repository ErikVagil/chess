package server;

import org.eclipse.jetty.websocket.api.*;

import webSocketMessages.userCommands.*;

public class UserCommandHandler {
    public static void handleJoinPlayer(Session session, JoinPlayerCommand command) {
        
    }

    public static void handleJoinObserver(Session session, JoinObserverCommand command) {

    }

    public static void handleMakeMove(Session session, MakeMoveCommand command) {

    }

    public static void handleLeave(Session session, LeaveCommand command) {

    }

    public static void handleResign(Session session, ResignCommand command) {

    }
}
